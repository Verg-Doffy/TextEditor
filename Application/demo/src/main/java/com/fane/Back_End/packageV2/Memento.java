package com.fane.Back_End.packageV2;
import com.fane.Back_End.packageV0.*;
import com.fane.Back_End.packageV1.*;

/**
 * The {@code Memento} interface represents an object used to store and retrieve the state
 * of another object, typically used for supporting undo and redo operations.
 *
 * Implementations of this interface may include methods for storing and retrieving state information,
 * allowing objects to capture and restore their internal state as needed.
 *
 * @author Mohamed AL AFTAN Djakaridja FANE
 * @version 2.0
 */
public interface Memento {
    // This could contain methods for storing and retrieving state,
    // but for simplicity, we'll keep it empty for now.
}
